<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="priv.mw.mapper.essay.EssayMapper">
    <insert id="addEssay" parameterType="essay">
        INSERT INTO essays(id, uid, name, content, tags, article, groupss)
        VALUES (#{id}, #{uid}, #{name}, #{content}, #{tags}, #{articleId}, #{groups,typeHandler=priv.mw.utils.mybatisTypeHandlers.GroupIdHandler})
    </insert>
</mapper>