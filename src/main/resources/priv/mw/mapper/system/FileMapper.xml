<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="priv.mw.mapper.system.FileMapper">
    <insert id="saveFile" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO assets(id, uid, uuid, name, types, createDate) VALUE (null, #{uId}, #{uuid}, #{name}, #{types}, #{createDate})
    </insert>
</mapper>